<!DOCTYPE html>
<html>
<head>
  <title>Rewslies</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>

<div style="float: right">
	<% if user_signed_in? %>
	Hi, <%= current_user.email %> | 
	<%= link_to "Log Out", destroy_user_session_path, :method => :delete %>
	<% else %>
	<a href="<%= new_user_session_path %>">Log In</a> | <a href="<%= new_user_registration_path %>">Sign Up</a>
	<% end %>
</div>

<h1><a href="<%= root_path %>">Welcome to Rewsly!</a></h1>

<%= notice %>
<%= alert %>


<%= yield %>

</body>
</html>
