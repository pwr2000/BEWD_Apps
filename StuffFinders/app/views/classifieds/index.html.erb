<div class="nav-header">Classifieds</div>

<div class='span9'>
<div class='well well-small'>
<ul class="nav nav-list">
  <% @classifieds.each do |classified| %>
	<li><a href="<%= classified_path(classified) %>"><%= classified.title %></a></li>
	<li><%= classified.description %></li>
	<li><%= classified.price %></li>
  <% end %>
</ul>
</div>
</div>

<div class='span2'>
<div class="well">
    <ul class="nav nav-list">
      <li class="nav-header">Categories</li>
      	<% @categories.each do |category| %>
			<li><a href="<%= category_path(category) %>"><%= category.name %></a></li>
  		<% end %>
  </ul>
</div>
</div>