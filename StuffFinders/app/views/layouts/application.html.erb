<!DOCTYPE html>
<html>
<head>
  <title>StuffFinders</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>

<div class="navbar navbar-static-top">
    <div class="navbar-inner">
      <ul class="nav pull-right">
        <% if user_signed_in? %>
        <li><a href="#">Hi, <%= current_user.email %></a></li>
        <li><%= link_to "Log Out", destroy_user_session_path, :method => :delete %></li>
        <% else %>
        <li><a href="<%= new_user_session_path %>">Log In</a></li>
        <li><a href="<%= new_user_registration_path %>">Sign Up</a></li>
        <% end %>
      </ul>
    </div>
  </div>


  <div class="navbar">
	  <div class="navbar-inner">
	    <a class="brand" href="<%= root_path %>">StuffFinder</a>
	    <ul class="nav">

	    	<li 
                <% if params[:controller] == "Classifieds" and params[:action] == "index" %> 
                class="active"
                <% end %>
                >
                <a href="<%= root_path %>">Browse Classifieds</a>
                </li>
                <li
                <% if params[:controller] == "Classifieds" and params[:action] == "new" %> 
                class="active"
                <% end %>
                >
                <a href="<%= new_classified_path %>">Post New Classified</a>
                </li>	      	
	    </ul>
	  </div>
   </div>

</head>
<body>
  <div class="container">
    <%= bootstrap_flash %>
    <%= yield %>
  </div>

</body>
</html>
